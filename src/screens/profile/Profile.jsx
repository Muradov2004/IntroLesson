import React from 'react';
import {StyledText, StyledView} from '@common/StyledComponents';

const Profile = () => {
  return (
    <StyledView>
      <StyledText className="text-black">Profile</StyledText>
    </StyledView>
  );
};

export default Profile;
