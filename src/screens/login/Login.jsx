import React from 'react';
import {StyledView, StyledText} from '@common/StyledComponents';

const Login = () => {
  return (
    <StyledView>
      <StyledText className="text-black">Login</StyledText>
    </StyledView>
  );
};

export default Login;
